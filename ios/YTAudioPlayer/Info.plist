<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>PeacePlayer</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>PeacePlayer</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>1.0</string>
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>com.ytaudioplayer.app</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>peaceplayer</string>
			</array>
		</dict>
	</array>
	<key>CFBundleVersion</key>
	<string>1</string>
	<key>NSAppTransportSecurity</key>
	<dict>
		<!-- S17 (CV-4): removed NSAllowsArbitraryLoads=true.
		     That key was a MITM vector — any URL the user
		     typed in Settings (LAN IP, Tailscale IP, but
		     also a malicious public HTTP server) would be
		     accepted, defeating ATS's HTTPS-only default
		     on iOS 9+. Instead we now allow ONLY local
		     networking (LAN + Tailscale / link-local) and
		     reject public HTTP. This is the iOS-blessed
		     way to support a personal backend on a
		     non-public routable address.

		     Production: the backend on the user's Mac is
		     always on a private address (Tailscale
		     100.x, LAN 192.168.x, or 10.x). Public
		     HTTPS-only is enforced for everything else.

		     If a future user does need to point the app
		     at a remote server, they should either use
		     HTTPS or add an explicit
		     NSExceptionDomains entry. -->
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
	<key>NSMotionUsageDescription</key>
	<string>Adaptive Walk DJ detects when you are walking so it can suggest a song at the right moment.</string>
	<!-- S15: enable Live Activities for the Now Playing Lock
	     Screen / Dynamic Island / StandBy integration. iOS 16.1+
	     only; the API is gated at the call site. -->
	<key>NSSupportsLiveActivities</key>
	<true/>
	<key>NSSupportsLiveActivitiesFrequentUpdates</key>
	<true/>
	<key>UIBackgroundModes</key>
	<array>
		<string>audio</string>
		<string>airplay</string>
	</array>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
