# Core dependencies
ytmusicapi==1.7.0
yt-dlp==2023.12.30
ffmpeg-python==0.2.0

# Web framework
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.6
aiofiles==23.2.1

# Utilities
requests==2.31.0
tinydb==4.8.0
pydantic==2.5.3

# Apple Sign-In (2026-06-28)
PyJWT>=2.8.0
cryptography>=41.0.0

# S17 (CV-3): load backend/.env for local development so the
# PEACEPLAYER_JWT_SECRET (and any other env vars) can be set
# in a file rather than exported in every shell. In production
# the launchd plist (or systemd unit) sets the env directly and
# .env is just ignored.
python-dotenv>=1.0.0
